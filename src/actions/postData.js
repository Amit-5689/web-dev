import axios from 'axios';

export const post = (model) => {
    // const url = `${process.env.REACT_APP_BASE_URL}/post-data`
    // return axios.post(url, model).then(res => {
    //     res.result
    // })
}